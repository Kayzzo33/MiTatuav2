
// AI Service Removed as per user request to prevent Vercel deployment crashes.
// This file is kept as a placeholder to prevent import errors in legacy code.

export const generateTattooConcept = async (idea: string, style: string): Promise<string | null> => {
  console.log("AI Feature disabled.");
  return null;
};
